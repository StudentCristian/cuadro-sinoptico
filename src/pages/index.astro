---
import Layout from '../layouts/Layout.astro';
import Card from '../components/Card.astro';
---

<Layout title="Visualización de Markmap">
    <main>
        <section class="header">
            <h1>Tarea 4: Algoritmos de Aprendizaje no Supervisado</h1>
            <p>
                <strong>Cuadro sinóptico</strong> sobre los diferentes modelos de aprendizaje no supervisado. <br />
                <span>Autor: Cristian Fontanilla</span> <br />
                <span>Curso: Análisis de Datos</span>
            </p>
        </section>
        <section class="markmap-container">
            <iframe
                src="/markmap.html"
                title="Visualización de Markmap"
                loading="lazy"
            ></iframe>
        </section>
    </main>
</Layout>

<style>
    :root {
        --primary-color: #007bff;
        --secondary-color: #6c757d;
        --background-color: #f8f9fa;
        --text-color: #333;
        --header-background: #e9ecef;
        --box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
        --border-radius: 10px;
        --transition: all 0.3s ease;
    }

    body {
        background-color: var(--background-color);
        color: var(--text-color);
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        margin: 0;
        padding: 0;
    }

    main {
        margin: auto;
        padding: 2rem;
        width: 90%;
        max-width: 1000px;
        background-color: #fff;
        border-radius: var(--border-radius);
        box-shadow: var(--box-shadow);
        transition: var(--transition);
    }

    main:hover {
        box-shadow: 0 6px 25px rgba(0, 0, 0, 0.15);
    }

    .header {
        text-align: center;
        margin-bottom: 2rem;
        padding: 1rem;
        background-color: var(--header-background);
        border-radius: var(--border-radius);
    }

    h1 {
        font-size: 2.5rem;
        margin-bottom: 0.5rem;
        color: var(--primary-color);
    }

    p {
        font-size: 1.2rem;
        color: var(--secondary-color);
    }

    .markmap-container {
        position: relative;
        width: 100%;
        height: 80vh;
        overflow: hidden;
        border-radius: var(--border-radius);
        box-shadow: var(--box-shadow);
        background-color: #fff;
        transition: var(--transition);
    }

    .markmap-container:hover {
        box-shadow: 0 6px 25px rgba(0, 0, 0, 0.15);
    }

    iframe {
        width: 100%;
        height: 100%;
        border: none;
        border-radius: var(--border-radius);
    }
</style>